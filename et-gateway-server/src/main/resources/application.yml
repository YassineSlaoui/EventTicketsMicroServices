spring:
    application:
        name: et-gateway-service
    config:
        import: "optional:configserver:"
    cloud:
        config:
            uri: http://localhost:8888
            name: et-gateway-service